<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Eventbrite Startup</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <%= javascript_include_tag 'application' %>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>    
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to "Events", root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= link_to "Home", root_path, class: "nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "Create a New Event", new_event_path, class: "nav-link" %>
      </li>
      <li class="nav-item">
        <% if user_signed_in? %>
        <%= link_to "Your Profile", edit_user_registration_path, class: "nav-link" %> <!--Devise propose un lien vers une page d'édition. Pour avoir mieux faire un autre controleur!-->
        <% end %>

      </li>

    </ul>
    <ul class="navbar-nav">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <% unless user_signed_in? %>
            Login / Sign Up
          <% else %>
            Logout
          <% end %>
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <div class="d-flex flex-column">
            <% unless user_signed_in? %>
              <%= link_to "Sign Up", new_user_registration_path, class: "dropdown-item" %>
              <%= link_to "Login", new_user_session_path, class: "dropdown-item" %>
            <% else %>
             <%= button_to "Se déconnecter", destroy_user_session_path, method: :delete, class: "nav-link", style: "color: #adb5bd;" %>

                <!-- Formulaire de déconnexion -->
                <%= form_tag(destroy_user_session_path, method: :delete) do %>
                  <%= button_tag(type: 'submit', class: 'nav-link', style: 'background: none; border: none; cursor: pointer;') do %>
                    Se déconnecter
                  <% end %>
                <% end %>
            
            <% end %>
          </div>
        </div>
      </li>
    </ul>
  </div>
</nav>

<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>

<%= yield %>

<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>
</html>
